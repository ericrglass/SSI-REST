CREATE TABLE MICRO_MARKET (
		ZIP_CODE VARCHAR(10) NOT NULL,
		RADIUS DOUBLE,
		AREA_LENGTH DOUBLE,
		AREA_WIDTH DOUBLE
	);

CREATE UNIQUE INDEX SQL130323125618500 ON MICRO_MARKET (ZIP_CODE ASC);

ALTER TABLE MICRO_MARKET ADD CONSTRAINT SQL130323125618500 PRIMARY KEY (ZIP_CODE);
