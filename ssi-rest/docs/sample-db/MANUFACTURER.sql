CREATE TABLE MANUFACTURER (
		MANUFACTURER_ID INTEGER NOT NULL,
		NAME VARCHAR(30),
		ADDRESSLINE1 VARCHAR(30),
		ADDRESSLINE2 VARCHAR(30),
		CITY VARCHAR(25),
		STATE CHAR(2),
		ZIP CHAR(10),
		PHONE VARCHAR(12),
		FAX VARCHAR(12),
		EMAIL VARCHAR(40),
		REP VARCHAR(30)
	);

CREATE UNIQUE INDEX SQL130323125600870 ON MANUFACTURER (MANUFACTURER_ID ASC);

ALTER TABLE MANUFACTURER ADD CONSTRAINT SQL130323125600870 PRIMARY KEY (MANUFACTURER_ID);
